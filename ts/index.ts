import * as vscode from 'vscode';

/**
 * Holds a place where there will one day be real APIs.
 *
 * @returns A placeholder
 */
export function placeholder(): string {
    console.log(vscode.extensions.all.length);
    return "placeholder";
}
